"""Masterplan source resolvers.

For MVP the only source is a local directory path. Future sources:
  - git: clone/pull a masterplan repo
  - http: fetch a JSON over HTTP (e.g. from a release artifact)
"""
